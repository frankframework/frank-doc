package org.frankframework.frankdoc.testtarget.config.child.no.i;

/**
 * We test that this class produces MyA. We remove the interface name "Child" and add the role name "A".
 * We cover the case that the interface name does not start with "I".
 * @author martijn
 *
 */
public class MyChild implements Child {
}
