import { MatchElementPipe } from './match-element.pipe';

describe('MatchElementPipe', () => {
  it('create an instance', () => {
    const pipe = new MatchElementPipe();
    expect(pipe).toBeTruthy();
  });
});
