import { TransformToRoutelinkDirective } from './transform-to-routelink.directive';

describe('TransformToRoutelinkDirective', () => {
  it('should create an instance', () => {
    const directive = new TransformToRoutelinkDirective();
    expect(directive).toBeTruthy();
  });
});
